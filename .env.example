# n8n Configuration
# Для localhost залиште значення як є, для продакшн вкажіть ваш домен/IP

# Хост n8n (для localhost залиште порожнім або localhost)
N8N_HOST=localhost

# Протокол (http для localhost, https для продакшн)
N8N_PROTOCOL=http

# URL для webhook (для localhost залиште порожнім або вкажіть http://localhost:5678/)
WEBHOOK_URL=http://localhost:5678/

# Часовий пояс (UTC, Europe/Kyiv, America/New_York, тощо)
GENERIC_TIMEZONE=UTC
